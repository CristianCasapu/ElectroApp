targets:
  $default:
    builders:
      drift_dev:
        options:
          # DateTime ca text ISO-8601 (cu milisecunde), nu ca secunde Unix —
          # jurnalul de stări și măsurătorile au nevoie de ordine sub-secundă.
          store_date_time_values_as_text: true
